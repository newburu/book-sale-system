= simple_form_for @user_writer do |f|
  = f.error_notification

  .form-inputs.table
    .row
      .col-sm-12
        = f.input :user_id, as: :hidden, input_html: {value: current_user.id}
    .row.align-items-center
      .col-sm-4
        = f.input :writer, readonly: true, input_html: {id: "writer", value: "#{@user_writer.writer.try(:name)}"}
        = f.input :writer_id, as: 'hidden', input_html: {id: "writer_id"}
      .col-sm-1
        = button_tag '選択', type: "button", class: "btn btn-lg btn-primary align-bottom", onclick: "open_window('writer')"

  hr
  
  .btn-toolbar
    .btn-group.mr-2
      .form-actions
        = f.button :submit, class: "btn btn-primary"
    .btn-group.mr-2
      = link_to t('simple_form.btn.back'), user_writers_path, class: "btn btn-secondary"
  
  hr
